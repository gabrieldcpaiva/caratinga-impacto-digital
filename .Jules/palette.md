## 2024-05-18 - Input Fields Missing Labels
**Learning:** Found an accessibility issue pattern across all business landing pages where inputs lacked explicitly associated labels (missing `for` attributes), breaking click-to-focus and screen reader compatibility.
**Action:** Adding proper `for` attributes to all existing `<label>` elements and ensuring every input has an associated label.
